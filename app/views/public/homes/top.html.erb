<div class="container-fuild">
  <div class="row">
    <div class="col-md-12">
      <div class="justify-content-center d-flex mt-3">
        <% @genres.each do |genre| %>
          <p class="btn-animation-02 ml-3"><span><%= link_to genre.name, posts_path(id: genre), class: 'btn btn-sm' %></span></p>
        <% end %>
      </div>
    </div>
  </div>
  <div class="row">
    <div class="col-md-12">
      <div class="main-visual">
        <div class="main-visual-content">
          <p class="text-white">捨てるなんてもったいない。</p>
          <p class="text-white">その食材を必要としている人がいます。</p>
        </div>
      </div>

      <h4 class="mt-3 ml-5">< 新着投稿 ></h4>
      <div class="text-center">
        <% @posts.each do |post| %>
        <table class= "d-inline-block ml-5 mr-3 mt-2">
          <tr>
            <td class="image-link">
              <%= link_to post_path(post) do %>
                <%= attachment_image_tag(post, :image, format: 'jpeg', size: "200x150") %>
              <% end %>
            </td>
          </tr>
          <tr>
            <td><%= post.name %></td>
          </tr>
          <tr>
            <td><%= post.price.to_s(:delimited) %>円</td>
          </tr>
        </table>
        <% end %>
      </div>

    </div>
  </div>
</div>