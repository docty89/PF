<div id="post_<%= @post.id %>">
  <%= attachment_image_tag @post, :image, :fill, 150, 150 %>
  <p>ジャンル：<%= @genre.name %></p>
  <p>食材名：<%= @post.name %></p>
  <p>価格：<%= @post.price.to_s(:delimited) %>円</p>
  <p>保存方法：<%= @post.storage %></p>
  <p>消費期限：<%= @post.expired %></p>
  <p>説明：<%= @post.body %></p>
  <p>投稿ユーザー画像:<%= link_to user_path(@post) do %><%= attachment_image_tag @post.user, :profile_image, :fill, 50, 50, format: 'jpeg', fallback: "no_image.jpeg" %><% end %></p>
  <p>ユーザーネーム：<%= @post.user.name %></p>
  <div class="favorite"><%= render "public/favorites/favorite", post: @post %></div>
  <p>投稿日：<%= l @post.created_at, format: :long %></p>
  <p>更新日：<%= l @post.updated_at, format: :long %></p>
  <% if @post.user == current_user %>
    <%= link_to "投稿削除", post_path(@post), method: :delete %>
    |
    <%= link_to "編集", edit_post_path(@post) %>
  <% end %>
</div>