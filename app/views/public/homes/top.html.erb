<div class="container-fuild">
  <div class="row">
    <div class="col-md-12">
      <div class="main-visual">
        <div class="main-visual-content">
          <p class="text-white top-text">捨てるなんてもったいない。</p>
          <p class="text-white top-text">その食材を必要としている人がいます。</p>
        </div>
      </div>
    </div>
  </div>
  <div class="row">
    <div class="col-md-12">
        <h3 class="mt-2 ml-2"><span style="background-color:#C0C0C0">[ 新着投稿 ]</span></h3>
        <div>
          <% @posts.each do |post| %>
          <table class= "d-inline-block ml-5 mr-3 mt-2">
            <tr>
              <td class="image-link">
                <%= link_to post_path(post) do %>
                  <%= attachment_image_tag(post, :image, format: 'jpeg', size: "200x160") %>
                <% end %>
              </td>
            </tr>
            <tr>
              <td><%= post.name %></td>
            </tr>
            <tr>
              <td><%= post.price.to_s(:delimited) %>円</td>
            </tr>
          </table>
          <% end %>
        </div>
    </div>
  </div>
</div>