<!---->
<!--index.html.erbのscript内に-->
<!--google mapのインスタンス変数である、-->
<!--mapという変数があることに留意-->
<!---->

(document).on('turbolinkfunction(){
  <!--// 位置情報をもつ変数を用意-->
  let pos = new google.maps.LatLng(
   <%= @latlng[0] %>,
   <%= @latlng[1] %>
  );

  mapInstance.setCenter(pos)
  let marker = new google.maps.Marker({
    map: mapInstance,
    position: pos
  });
})()